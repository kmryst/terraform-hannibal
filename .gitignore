# compiled output
/dist
/node_modules
/client/dist
/client/node_modules

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS
.DS_Store

# Tests
/coverage
/.nyc_output

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

.terraform/
.artifacts/
**/.artifacts/
*.tfvars
*.tfvars.json

# Terraform state（リモートstateバックエンドを使うため、ローカルにstateファイルが
# 生成されるのは異常系のみだが、誤コミットを防ぐため明示的に除外する。Issue #507）
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Build analysis
stats.html
.github_all_files.txt
current_template.yml
*.bak

# Personal TODO management
TODO.md

memo/
drafts/
docs/worklogs/

# Local Codex workspace file
.codex

# Local Claude Code settings
.claude/settings.local.json

# Local mise overrides
mise.local.toml
.mise.local.toml

# 環境変数ファイル（ランタイムは ECS タスク定義から注入。.env.example のみ管理）
.env
.env.*
!.env.example
!**/.env.example

# LLM 向けリポジトリ連結（この配下はローカル専用・Git 管理外）
# docs/llm-repo-bundle/repository-bundle.txt と exclude-rules.md を手元に置く
docs/llm-repo-bundle/

# =============================
# ⚠️ 重要: CloudFrontディストリビューションはterraform destroy前に必ず手動で削除してください！
# AWSマネジメントコンソール→CloudFront→該当ディストリビューション→Disable→Delete
# これを行わないと循環参照エラーでdestroyに失敗します。
# =============================

name: 🗑️ Destroy AWS Infrastructure (Dev)

on:
  workflow_dispatch: # 手動実行のみ
    inputs:
      environment:
        description: 'Which environment to destroy?'
        required: true
        default: 'all'
        type: choice
        options:
          - frontend
          - backend
          - all
      confirm_destroy:
        description: 'Type "DESTROY" to confirm deletion'
        required: true
        type: string
      double_confirm:
        description: 'Type project name "nestjs-hannibal-3" to double confirm'
        required: true
        type: string

concurrency:
  group: terraform-destroy
  cancel-in-progress: false  # destroy中断は危険

jobs:
  destroy:
    runs-on: ubuntu-latest
    if: >
      github.event.inputs.confirm_destroy == 'DESTROY' && 
      github.event.inputs.double_confirm == 'nestjs-hannibal-3'
    steps:
      - name: 🛑 Destruction Warning
        run: |
          echo "⚠️  WARNING: This will PERMANENTLY DELETE AWS resources!"
          echo "📋 Environment: ${{ github.event.inputs.environment }}"
          echo "💰 This will stop AWS billing for this project (except for ECR/S3 tfstate/CloudTrail logs/OAC)"
          echo "🔄 Resources can be recreated later with terraform apply"
          echo "❗ ECRリポジトリ・S3バケット（tfstate・CloudTrailログ）・OACは絶対に消しません！"

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials (Development)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-1
      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq
        
      - name: Assume CICD Role for Cleanup
        run: |
          CICD_CREDS=$(aws sts assume-role --role-arn arn:aws:iam::258632448142:role/HannibalCICDRole-Dev --role-session-name github-actions-destroy)
          echo "AWS_ACCESS_KEY_ID=$(echo $CICD_CREDS | jq -r '.Credentials.AccessKeyId')" >> $GITHUB_ENV
          echo "AWS_SECRET_ACCESS_KEY=$(echo $CICD_CREDS | jq -r '.Credentials.SecretAccessKey')" >> $GITHUB_ENV
          echo "AWS_SESSION_TOKEN=$(echo $CICD_CREDS | jq -r '.Credentials.SessionToken')" >> $GITHUB_ENV

      - name: Cache Terraform
        uses: actions/cache@v4
        with:
          path: |
            ~/.terraform.d/plugin-cache
            **/.terraform
          key: ${{ runner.os }}-terraform-${{ hashFiles('**/*.tf') }}
          restore-keys: |
            ${{ runner.os }}-terraform-

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.0.0
          terraform_wrapper: false

      # ⚠️ 競合しやすいリソースを事前削除（Terraform destroyの成功率向上のため）
      # 理由: ECSサービス、ALB、セキュリティグループなどは依存関係でTerraform destroyが失敗しやすい
      # 順番: 依存関係の逆順（下流→上流）で削除
      # エラー処理: 2>/dev/null || true でリソースが存在しない場合もエラーにしない
      - name: Clean up existing AWS resources (ECS/ALB/SG)
        run: |
          # 0. Professional設計: プロジェクト名とTerraformのoutputから動的に値を取得
          PROJECT_NAME="nestjs-hannibal-3"
          cd ./terraform/backend
          if [ -f "terraform.tfstate" ] && [ -s "terraform.tfstate" ]; then
            CODEDEPLOY_APP=$(terraform output -raw codedeploy_application_name 2>/dev/null || echo "$PROJECT_NAME-ecs-app")
            CODEDEPLOY_GROUP=$(terraform output -raw codedeploy_deployment_group_name 2>/dev/null || echo "$PROJECT_NAME-deployment-group")
            BLUE_SERVICE=$(terraform output -raw blue_service_name 2>/dev/null || echo "$PROJECT_NAME-blue-service")
            GREEN_SERVICE=$(terraform output -raw green_service_name 2>/dev/null || echo "$PROJECT_NAME-green-service")
            CLUSTER_NAME=$(terraform output -raw ecs_cluster_name 2>/dev/null || echo "$PROJECT_NAME-cluster")
          else
            echo "ℹ️ Terraform state not found, using default names"
            CODEDEPLOY_APP="$PROJECT_NAME-ecs-app"
            CODEDEPLOY_GROUP="$PROJECT_NAME-deployment-group"
            BLUE_SERVICE="$PROJECT_NAME-blue-service"
            GREEN_SERVICE="$PROJECT_NAME-green-service"
            CLUSTER_NAME="$PROJECT_NAME-cluster"
          fi
          cd ../..
          
          echo "ℹ️ Using CodeDeploy App: $CODEDEPLOY_APP"
          echo "ℹ️ Using CodeDeploy Group: $CODEDEPLOY_GROUP"
          echo "ℹ️ Using Blue Service: $BLUE_SERVICE"
          echo "ℹ️ Using Green Service: $GREEN_SERVICE"
          echo "ℹ️ Using Cluster: $CLUSTER_NAME"
          
          # 1. CodeDeploy削除（AWS Professional Blue/Green - 依存関係順序重要）
          echo "🗑️ Cleaning up CodeDeploy deployment group..."
          if aws deploy get-deployment-group --application-name $CODEDEPLOY_APP --deployment-group-name $CODEDEPLOY_GROUP --region ap-northeast-1 >/dev/null 2>&1; then
            aws deploy delete-deployment-group --application-name $CODEDEPLOY_APP --deployment-group-name $CODEDEPLOY_GROUP --region ap-northeast-1
            echo "✅ CodeDeploy deployment group deleted"
          else
            echo "ℹ️ CodeDeploy deployment group not found (already deleted)"
          fi
          
          echo "🗑️ Cleaning up CodeDeploy application..."
          if aws deploy get-application --application-name $CODEDEPLOY_APP --region ap-northeast-1 >/dev/null 2>&1; then
            aws deploy delete-application --application-name $CODEDEPLOY_APP --region ap-northeast-1
            echo "✅ CodeDeploy application deleted"
          else
            echo "ℹ️ CodeDeploy application not found (already deleted)"
          fi
          
          # 2. Blue/Green ECSサービス削除（Professional設計対応）
          echo "🗑️ Cleaning up Green ECS service..."
          aws ecs update-service --cluster $CLUSTER_NAME --service $GREEN_SERVICE --desired-count 0 --region ap-northeast-1 2>/dev/null || true
          sleep 30
          aws ecs delete-service --cluster $CLUSTER_NAME --service $GREEN_SERVICE --force --region ap-northeast-1 2>/dev/null || true
          
          # 3. Blue ECSサービス削除
          echo "🗑️ Cleaning up Blue ECS service..."
          aws ecs update-service --cluster $CLUSTER_NAME --service $BLUE_SERVICE --desired-count 0 --region ap-northeast-1 2>/dev/null || true
          sleep 30  # ECSタスクの完全停止を待つ
          aws ecs delete-service --cluster $CLUSTER_NAME --service $BLUE_SERVICE --force --region ap-northeast-1 2>/dev/null || true

          # 4. ECSクラスタ削除（全サービス削除後に実行）
          echo "🗑️ Cleaning up ECS cluster..."
          aws ecs delete-cluster --cluster $CLUSTER_NAME --region ap-northeast-1 2>/dev/null || true

          # 3. ALB削除（ターゲットグループが残っていると失敗するため先に削除）
          echo "🗑️ Cleaning up ALB..."
          ALB_ARN=$(aws elbv2 describe-load-balancers --names $PROJECT_NAME-alb --region ap-northeast-1 --query 'LoadBalancers[0].LoadBalancerArn' --output text 2>/dev/null || echo "None")
          if [ "$ALB_ARN" != "None" ] && [ "$ALB_ARN" != "null" ]; then
            aws elbv2 delete-load-balancer --load-balancer-arn "$ALB_ARN" --region ap-northeast-1
            sleep 60  # ALB削除完了を待つ（時間がかかるため）
          fi

          # 4. Blue/Greenターゲットグループ削除（ALB削除後に実行）
          echo "🗑️ Cleaning up Blue/Green target groups..."
          BLUE_TG_ARN=$(aws elbv2 describe-target-groups --names $PROJECT_NAME-blue-tg --region ap-northeast-1 --query 'TargetGroups[0].TargetGroupArn' --output text 2>/dev/null || echo "None")
          if [ "$BLUE_TG_ARN" != "None" ] && [ "$BLUE_TG_ARN" != "null" ]; then
            aws elbv2 delete-target-group --target-group-arn "$BLUE_TG_ARN" --region ap-northeast-1
          fi
          
          GREEN_TG_ARN=$(aws elbv2 describe-target-groups --names $PROJECT_NAME-green-tg --region ap-northeast-1 --query 'TargetGroups[0].TargetGroupArn' --output text 2>/dev/null || echo "None")
          if [ "$GREEN_TG_ARN" != "None" ] && [ "$GREEN_TG_ARN" != "null" ]; then
            aws elbv2 delete-target-group --target-group-arn "$GREEN_TG_ARN" --region ap-northeast-1
          fi

          sleep 30  # 依存関係のクリアを待つ

          # 5. セキュリティグループ削除（ECSタスクやALBが削除された後に実行）
          echo "🗑️ Cleaning up security groups..."
          SG_ECS_ID=$(aws ec2 describe-security-groups --group-names $PROJECT_NAME-ecs-service-sg --region ap-northeast-1 --query 'SecurityGroups[0].GroupId' --output text 2>/dev/null || echo "None")
          if [ "$SG_ECS_ID" != "None" ] && [ "$SG_ECS_ID" != "null" ]; then
            aws ec2 delete-security-group --group-id "$SG_ECS_ID" --region ap-northeast-1 2>/dev/null || true
          fi

          SG_ALB_ID=$(aws ec2 describe-security-groups --group-names $PROJECT_NAME-alb-sg --region ap-northeast-1 --query 'SecurityGroups[0].GroupId' --output text 2>/dev/null || echo "None")
          if [ "$SG_ALB_ID" != "None" ] && [ "$SG_ALB_ID" != "null" ]; then
            aws ec2 delete-security-group --group-id "$SG_ALB_ID" --region ap-northeast-1 2>/dev/null || true
          fi

          # 6. IAMロール削除（Professional Blue/Green対応）
          echo "🗑️ Cleaning up IAM roles..."
          # ECS Task Execution Role
          aws iam detach-role-policy --role-name $PROJECT_NAME-ecs-task-execution-role --policy-arn arn:aws:iam::aws:policy/service-role/AmazonECSTaskExecutionRolePolicy 2>/dev/null || true
          aws iam delete-role --role-name $PROJECT_NAME-ecs-task-execution-role 2>/dev/null || true
          # CodeDeploy Service Role
          aws iam detach-role-policy --role-name $PROJECT_NAME-codedeploy-service-role --policy-arn arn:aws:iam::aws:policy/AWSCodeDeployRoleForECS 2>/dev/null || true
          aws iam delete-role --role-name $PROJECT_NAME-codedeploy-service-role 2>/dev/null || true

          # 7. RDS削除（セキュリティグループ削除前に実行）
          echo "🗑️ Cleaning up RDS instance..."
          aws rds delete-db-instance --db-instance-identifier $PROJECT_NAME-postgres --skip-final-snapshot --region ap-northeast-1 2>/dev/null || true
          echo "⏳ Waiting for RDS deletion (this may take several minutes)..."
          sleep 120  # RDS削除には時間がかかる

          # 8. RDSサブネットグループ削除
          echo "🗑️ Cleaning up RDS subnet group..."
          aws rds delete-db-subnet-group --db-subnet-group-name $PROJECT_NAME-db-subnet-group --region ap-northeast-1 2>/dev/null || true

          # 9. RDSセキュリティグループ削除
          echo "🗑️ Cleaning up RDS security group..."
          SG_RDS_ID=$(aws ec2 describe-security-groups --group-names $PROJECT_NAME-rds-sg --region ap-northeast-1 --query 'SecurityGroups[0].GroupId' --output text 2>/dev/null || echo "None")
          if [ "$SG_RDS_ID" != "None" ] && [ "$SG_RDS_ID" != "null" ]; then
            aws ec2 delete-security-group --group-id "$SG_RDS_ID" --region ap-northeast-1 2>/dev/null || true
          fi

          # 10. 監視リソース削除（CloudWatch Alarms, SNS Topic, Dashboard）
          echo "🗑️ Cleaning up monitoring resources..."
          
          # CloudWatch Alarms削除（AWS Professional Blue/Green監視対応）
          aws cloudwatch delete-alarms --alarm-names \
            "$PROJECT_NAME-ecs-cpu-high-blue" \
            "$PROJECT_NAME-ecs-cpu-high-green" \
            "$PROJECT_NAME-ecs-memory-high-blue" \
            "$PROJECT_NAME-ecs-memory-high-green" \
            "$PROJECT_NAME-ecs-task-stopped-active" \
            "$PROJECT_NAME-deployment-health" \
            "$PROJECT_NAME-deployment-health-green" \
            "$PROJECT_NAME-rds-cpu-high" \
            "$PROJECT_NAME-rds-connections-high" \
            "$PROJECT_NAME-alb-response-time-high" \
            "$PROJECT_NAME-alb-5xx-error-rate-high" \
            --region ap-northeast-1 2>/dev/null || true
          
          # CloudWatch Dashboard削除
          aws cloudwatch delete-dashboards --dashboard-names "hannibal-system-dashboard" --region ap-northeast-1 2>/dev/null || true
          
          # SNS Topic削除
          SNS_TOPIC_ARN=$(aws sns list-topics --region ap-northeast-1 --query "Topics[?contains(TopicArn, \`$PROJECT_NAME-alerts\`)].TopicArn" --output text 2>/dev/null || echo "None")
          if [ "$SNS_TOPIC_ARN" != "None" ] && [ "$SNS_TOPIC_ARN" != "" ]; then
            aws sns delete-topic --topic-arn "$SNS_TOPIC_ARN" --region ap-northeast-1 2>/dev/null || true
          fi

          # 11. CloudWatchロググループ削除（ECSタスク削除後に実行）
          echo "🗑️ Cleaning up CloudWatch log group..."
          aws logs delete-log-group --log-group-name /ecs/$PROJECT_NAME-api-task --region ap-northeast-1 2>/dev/null || true

          # 12. CloudTrail削除（S3ログは保持）
          echo "🗑️ Cleaning up CloudTrail (keeping S3 logs for audit)..."
          aws cloudtrail delete-trail --name $PROJECT_NAME-terraform-trail --region ap-northeast-1 2>/dev/null || true

          # 13. Access Analyzer削除
          echo "🗑️ Cleaning up Access Analyzer..."
          aws accessanalyzer delete-analyzer --analyzer-name $PROJECT_NAME-analyzer --region ap-northeast-1 2>/dev/null || true

          echo "✅ CLI cleanup completed - Terraform destroy can now proceed safely"
          echo "📋 CloudTrail logs preserved in S3 for security audit purposes"


      # フロントエンド削除
      - name: 🎨 Destroy Frontend Infrastructure
        if: github.event.inputs.environment == 'frontend' || github.event.inputs.environment == 'all'
        run: |
          echo "🗑️ Destroying frontend resources..."
          yes | terraform init
          
          # CloudFront state cleanup (Professional設計: 手動削除後のstate同期)
          echo "🔧 Cleaning up CloudFront from Terraform state..."
          terraform state rm aws_cloudfront_distribution.main 2>/dev/null || true
          
          # Terraform destroy実行（CloudFront無効化）
          terraform destroy -auto-approve -var="api_alb_dns_name=dummy" -var="enable_cloudfront=false"
          echo "✅ Frontend infrastructure destroyed"
        working-directory: ./terraform/frontend


      # バックエンド削除
      - name: 🔧 Destroy Backend Infrastructure
        if: github.event.inputs.environment == 'backend' || github.event.inputs.environment == 'all'
        run: |
          echo "🗑️ Destroying backend resources..."
          yes | terraform init
          terraform destroy -auto-approve -var="client_url_for_cors="
          echo "✅ Backend infrastructure destroyed"
        working-directory: ./terraform/backend

      - name: 🎉 Cleanup Complete
        run: |
          echo "🎉 Infrastructure cleanup completed!"
          echo "💰 AWS billing should stop within 1-2 hours (except for ECR/S3 tfstate/CloudTrail logs/OAC)"
          echo "📊 Check AWS Cost Explorer to confirm"
          echo ""
          echo "🔄 To redeploy later:"
          echo "  1. Push to feature/github-actions branch"
          echo "  2. Or run terraform apply manually"
